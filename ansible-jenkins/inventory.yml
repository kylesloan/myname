# inventory.yml
[myproject:children]
jenkins

[jenkins]
jenkins001 ansible_host="{{ aws.instance_ip }}" ansible_user="{{ aws.instance_user }}" ansible_ssh_private_key_file="{{ aws.instance_ssh_key }}"
